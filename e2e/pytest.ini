[pytest]
testpaths = tests
asyncio_mode = auto
timeout = 120
markers =
    ui: Playwright browser tests
    api: REST API tests
addopts = --tb=long --junitxml=artifacts/junit.xml --html=artifacts/report.html -v
